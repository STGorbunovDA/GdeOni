<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GdeOni.Mobile.Views.Shared.AppBottomBar">

    <Grid>
        <Border BackgroundColor="{AppThemeBinding Light={StaticResource Cloud}, Dark={StaticResource CloudDark}}"
                StrokeThickness="0"
                Margin="16,0,16,16"
                Padding="8">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="32" />
            </Border.StrokeShape>
            <Border.Shadow>
                <Shadow Brush="{StaticResource InkBlueBrush}"
                        Offset="0,4"
                        Radius="14"
                        Opacity="0.12" />
            </Border.Shadow>

            <Grid ColumnDefinitions="*,*,*,*"
                  HeightRequest="60">

                <!-- Поиск (список отслеживаемых карточек) -->
                <Button x:Name="TabTracked"
                        Grid.Column="0"
                        Text="Поиск"
                        Clicked="OnTrackedTapped"
                        FontFamily="Raleway"
                        FontSize="11"
                        TextColor="#9AA8B5"
                        BackgroundColor="Transparent"
                        BorderWidth="0"
                        CornerRadius="24"
                        Padding="0" />

                <!-- События -->
                <Button x:Name="TabEvents"
                        Grid.Column="1"
                        Text="События"
                        Clicked="OnEventsTapped"
                        FontFamily="Raleway"
                        FontSize="11"
                        TextColor="#9AA8B5"
                        BackgroundColor="Transparent"
                        BorderWidth="0"
                        CornerRadius="24"
                        Padding="0" />

                <!-- Маршрут -->
                <Button x:Name="TabRoute"
                        Grid.Column="2"
                        Text="Маршрут"
                        Clicked="OnRouteTapped"
                        FontFamily="Raleway"
                        FontSize="11"
                        TextColor="#9AA8B5"
                        BackgroundColor="Transparent"
                        BorderWidth="0"
                        CornerRadius="24"
                        Padding="0" />

                <!-- Профиль -->
                <Button x:Name="TabProfile"
                        Grid.Column="3"
                        Text="Профиль"
                        Clicked="OnProfileTapped"
                        FontFamily="Raleway"
                        FontSize="11"
                        TextColor="#9AA8B5"
                        BackgroundColor="Transparent"
                        BorderWidth="0"
                        CornerRadius="24"
                        Padding="0" />
            </Grid>
        </Border>
    </Grid>

</ContentView>
